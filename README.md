# Sweet Tooth - Restaurant Website

Современный веб-сайт кондитерской "Sweet Tooth" с интерактивными анимациями и плавной прокруткой.

## 🎯 Описание

Веб-приложение для кондитерской с использованием Vue 3, TypeScript и GSAP для создания плавных анимаций при прокрутке страницы. Проект включает Hero-секцию с анимацией 3D-моделей продуктов, интерактивную галерею товаров и современный адаптивный дизайн.

## 🚀 Технологии

- **Vue 3** - Прогрессивный JavaScript-фреймворк
- **TypeScript** - Типизированный JavaScript
- **Vite** - Быстрый сборщик и dev-сервер
- **GSAP** - Библиотека для создания высокопроизводительных анимаций
- **ScrollTrigger** - Плагин GSAP для анимаций на основе прокрутки
- **Tailwind CSS** - Utility-first CSS фреймворк
- **ESLint** - Линтер для JavaScript/TypeScript
- **Prettier** - Форматтер кода
- **Husky** - Git hooks для автоматизации

## 📋 Требования

- **Node.js** >= 18.x
- **Bun** >= 1.3.x (или npm/yarn)

## 🛠️ Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Melik1986/Restouarand.git
cd Restouarand/web-app
```

2. Установите зависимости:
```bash
bun install
```

## 🎮 Запуск проекта

### Режим разработки
```bash
bun run dev
```
Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для production
```bash
bun run build
```
Собранные файлы будут в директории `dist/`

### Предпросмотр production сборки
```bash
bun run preview
```

## 📜 Доступные скрипты

- `bun run dev` - Запуск dev-сервера
- `bun run build` - Сборка проекта для production
- `bun run preview` - Предпросмотр production сборки
- `bun run lint` - Проверка кода линтером
- `bun run lint:fix` - Автоматическое исправление ошибок линтера
- `bun run type-check` - Проверка типов TypeScript
- `bun run format` - Форматирование кода с помощью Prettier
- `bun run format:check` - Проверка форматирования кода

## 📁 Структура проекта

```
web-app/
├── public/                 # Статические файлы
│   ├── animation/          # Кадры анимаций моделей (model-1 до model-6)
│   ├── backgraund.png     # Фоновое изображение для Hero секции
│   ├── Interior.jpg       # Изображение интерьера
│   ├── map.svg            # SVG карты мира
│   └── title.svg          # SVG логотипа/заголовка
├── src/
│   ├── components/
│   │   ├── features/       # Функциональные компоненты
│   │   │   ├── ProductCard.vue      # Карточка товара с анимацией
│   │   │   ├── ProductsGrid.vue    # Сетка товаров с пинингом
│   │   │   └── VideoScrollHero.vue # Hero секция с моделями
│   │   ├── layout/        # Компоненты макета
│   │   │   ├── TheFooter.vue       # Футер сайта
│   │   │   └── TheNavbar.vue       # Навигационная панель
│   │   └── ui/            # UI компоненты
│   │       ├── SectionTitle.vue     # Заголовок секции
│   │       ├── UiButton.vue        # Кнопка
│   │       └── icons/              # SVG иконки
│   ├── views/
│   │   └── HomeView.vue   # Главная страница
│   ├── style.css          # Глобальные стили
│   └── main.ts            # Точка входа приложения
├── .eslintrc.js           # Конфигурация ESLint
├── .prettierrc            # Конфигурация Prettier
├── tailwind.config.js     # Конфигурация Tailwind CSS
├── tsconfig.json          # Конфигурация TypeScript
└── vite.config.ts         # Конфигурация Vite
```

## ✨ Основные возможности

### 🎬 Анимации при прокрутке

- **Hero секция**: Последовательная анимация двух 3D-моделей продуктов с плавными переходами
- **Сетка товаров**: Пиннинг секции с синхронизированной анимацией вращения моделей
- **Плавное появление**: Все элементы страницы (заголовки, карточки, футер) плавно появляются при прокрутке
- **Stagger эффекты**: Каскадное появление элементов с задержкой

### 🎨 Компоненты

- **VideoScrollHero**: Hero-секция с canvas-анимацией моделей, управляемой прокруткой
- **ProductsGrid**: Сетка товаров с пинингом и анимацией кадров моделей
- **ProductCard**: Карточка товара с поддержкой анимации кадров
- **SectionTitle**: Переиспользуемый компонент заголовка секции с анимацией

### 🎯 Особенности реализации

- Использование GSAP ScrollTrigger для синхронизации анимаций с прокруткой
- Canvas-рендеринг для плавной анимации больших изображений
- Адаптивный дизайн с поддержкой мобильных устройств
- Оптимизация производительности через ленивую загрузку изображений
- TypeScript для типобезопасности

## 🔧 Конфигурация

### Tailwind CSS
Настроен с кастомными цветами и типографикой для брендинга "Sweet Tooth".

### ESLint
Настроен с правилами для Vue 3, TypeScript и лучших практик разработки.

### Prettier
Автоматическое форматирование кода при коммите через Husky hooks.

## 📝 Стиль кода

Проект следует стандартам:
- **TypeScript strict mode** - Строгая проверка типов
- **Vue 3 Composition API** - Использование `<script setup>`
- **BEM методология** - Для SCSS (где используется)
- **Conventional Commits** - Формат сообщений коммитов

## 🚀 Деплой

Проект готов к деплою на:
- **Vercel** - Рекомендуется для Vue приложений
- **Netlify** - Простой деплой через Git
- **GitHub Pages** - Статический хостинг

Для деплоя выполните:
```bash
bun run build
```
И загрузите содержимое папки `dist/` на хостинг.

## 📄 Лицензия

Проект является частным.

## 👥 Авторы

Разработано для кондитерской "Sweet Tooth"

---

**Примечание**: Для работы анимаций требуется загрузка всех кадров моделей из папки `public/animation/`. Убедитесь, что все файлы присутствуют перед деплоем.
